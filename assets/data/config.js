/* ============================================================
   SFUA — LA Regional Food Network
   Live database connection

   Paste your Supabase project's URL and anon key below, then
   commit and push. Until both values are filled in, the page
   runs in prototype mode (entries save in the browser only).

   Full instructions: SETUP-LIVE-DATABASE.md in the repo root.

   The anon key is safe to publish — it only permits what the
   database's security policies allow, and those require a
   signed-in partner account for every view and edit.
   ============================================================ */

window.SFUA_CONFIG = {
  SUPABASE_URL: "",      // e.g. "https://abcdefghijkl.supabase.co"
  SUPABASE_ANON_KEY: ""  // the long "anon / public" key
};
