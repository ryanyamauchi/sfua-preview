# Netlify configuration — SFUA Food Network (static site)
# Connect this repo in the Netlify dashboard and deploy; no build step.

[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
